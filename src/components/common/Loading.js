import logo from '../../assets/logo.png';
import './Loading.css';

function Loading() {
    return (
       <img className='Loading' src={logo} alt='Kontrarian Asset Management' />
    );
}

export default Loading;